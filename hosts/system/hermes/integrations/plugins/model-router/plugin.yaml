name: model-router
version: 0.1.0
description: >-
  Per-turn cost router: DeepSeek Flash (T1), DeepSeek Pro (T2), Grok 4.6 (T3).
  Classifies via auxiliary.triage_specifier (already Flash). Pins with /t1 /t2 /t3 /auto.
  Uses AIAgent.switch_model — no Hermes/WebUI core edits.
author: aean0x
kind: standalone
provides_hooks:
  - pre_llm_call
  - post_llm_call
  - post_tool_call
  - pre_api_request
