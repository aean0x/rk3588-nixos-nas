name: gbrain-retrieval-reflex
version: 0.4.0
description: >
  Ambient GBrain push-context: HTTP MCP volunteer_context (entity, multi-turn)
  + hybrid query fallback, merge/dedupe, inject top-5 strength-ordered Brain
  pages with 50–400 char previews into pre_llm_call. Optional resolve IPC if
  sock exists (stdio serve only). HTTP sole-owner topology on rocknas.
author: aean0x
kind: standalone
provides_hooks:
  - pre_llm_call
