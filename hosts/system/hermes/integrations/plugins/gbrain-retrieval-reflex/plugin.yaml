name: gbrain-retrieval-reflex
version: 0.3.0
description: >
  Ambient GBrain push-context: call HTTP MCP volunteer_context (Bearer) and
  inject ## Brain pages into pre_llm_call. Optional resolve IPC if sock exists
  (stdio serve only). HTTP sole-owner topology on rocknas.
author: aean0x
kind: standalone
provides_hooks:
  - pre_llm_call
