#!/usr/bin/env bash
# Git credential helper: feeds GITHUB_PAT / GH_TOKEN from the environment.
# Configure once:
#   git config --global credential.helper /data/.hermes/scripts/git-credential-github-env
#   git config --global credential.useHttpPath true
set -euo pipefail
# Only respond to get; store/erase are no-ops (token is ephemeral env).
op="${1:-get}"
case "$op" in
  get)
    token="${GITHUB_PAT:-${GH_TOKEN:-${GITHUB_TOKEN:-}}}"
    if [[ -z "$token" ]]; then
      exit 0
    fi
    # Read protocol/host from stdin (git credential protocol)
    host=""; protocol=""
    while IFS= read -r line; do
      [[ -z "$line" ]] && break
      case "$line" in
        host=*) host="${line#host=}" ;;
        protocol=*) protocol="${line#protocol=}" ;;
      esac
    done
    if [[ "$host" != "github.com" && -n "$host" ]]; then
      exit 0
    fi
    printf 'username=x-access-token\npassword=%s\n' "$token"
    ;;
  store|erase)
    exit 0
    ;;
  *)
    exit 0
    ;;
esac
